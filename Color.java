/*
 * An enum with two values,
 *    WHITE and BLACK, which represent the colors of the chess pieces.
 * This enum keeps track of whether it is White or Black's move
 */

public enum Color {
    WHITE, BLACK;
}
